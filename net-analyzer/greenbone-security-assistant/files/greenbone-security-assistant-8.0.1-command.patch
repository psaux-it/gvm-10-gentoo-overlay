--- a/gsa/CMakeLists.txt	2019-04-12 19:06:39.267569820 +0300
+++ b/gsa/CMakeLists.txt	2019-04-12 19:10:31.837282106 +0300
@@ -1092,13 +1092,13 @@
      PARENT_SCOPE)
 
 add_custom_command (OUTPUT bundle.stamp
-                    COMMAND ${INSTALLER} "${INSTALLER_ARGS}" run build
-                    COMMAND ${CMAKE_COMMAND} -E touch ${CMAKE_CURRENT_BINARY_DIR}/bundle.stamp
-                    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
+                    COMMAND ${INSTALLER} "${INSTALLER_ARGS}" run build &>/dev/null
+                    COMMAND ${CMAKE_COMMAND} -E touch ${CMAKE_CURRENT_BINARY_DIR}/bundle.stamp &>/dev/null
+                    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
                     DEPENDS  ${GSA_PKG_FILES}
                              ${GSA_STATIC_SRC_FILES}
                              ${GSA_JS_SRC_FILES}
-                    COMMENT "Build gsa-ng install files")
+                    COMMENT "Yarn creating an optimized production build.This will take some time.")
 
 add_custom_target (gsa-ng ALL
                    DEPENDS ${CMAKE_CURRENT_BINARY_DIR}/bundle.stamp)
